{
  "name": "alter-co",
  "version": "1.0.0",
  "description": "ALTER.CO – app and API in one",
  "scripts": {
    "build": "npm install --prefix backend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
